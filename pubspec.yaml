name: config_panel_flutter
description: "Device Configuration Control Panel"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  go_router: ^15.1.2
  provider: ^6.1.5
  grpc: ^5.1.0
  protobuf: ^6.0.0
  fixnum: ^1.1.0
  intl: ^0.20.2
  flutter_dotenv: ^5.2.1
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
